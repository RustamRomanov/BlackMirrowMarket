# 🔧 Принудительное использование Railpack в Railway

## Проблема

Railway автоматически определил Dockerfile и заблокировал возможность изменить Builder - выпадающий список не открывается.

## Решение

### Что было сделано:

1. ✅ **Dockerfile.backup удален** из репозитория - Railway больше не видит Dockerfile
2. ✅ **railway.json настроен** на использование Railpack
3. ✅ **nixpacks.toml добавлен** для принудительного использования Nixpacks

### Что нужно сделать в Railway:

#### Вариант 1: Пересоздать сервис (РЕКОМЕНДУЕТСЯ)

1. В Railway откройте ваш проект
2. Найдите сервис **BlackMirrowMarket**
3. Нажмите на **три точки** (меню) рядом с сервисом
4. Выберите **"Delete"** или **"Remove"**
5. **НЕ УДАЛЯЙТЕ PostgreSQL!** Только сервис BlackMirrowMarket
6. Создайте новый сервис:
   - Нажмите **"+ New"** → **"GitHub Repo"**
   - Выберите репозиторий **RustamRomanov/BlackMirrowMarket**
   - Railway создаст новый сервис БЕЗ Dockerfile
   - Настройте:
     - Root Directory: `backend`
     - Builder: автоматически выберет Railpack
     - Start Command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

#### Вариант 2: Подождать и проверить

1. Подождите 2-3 минуты после удаления Dockerfile.backup
2. Обновите страницу Railway (F5)
3. Попробуйте снова нажать на Builder - возможно, теперь откроется
4. Если нет - используйте Вариант 1

#### Вариант 3: Использовать Railway CLI

Если у вас установлен Railway CLI:

```bash
railway login
railway link
railway variables set BUILDER=railpack
```

---

## 📝 Настройки нового сервиса:

После создания нового сервиса:

1. **Root Directory:** `backend`
2. **Builder:** Railpack (автоматически)
3. **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
4. **Variables:** добавьте все переменные окружения (см. список ниже)

---

## ⚠️ Важно:

- **НЕ УДАЛЯЙТЕ PostgreSQL!** Только сервис BlackMirrowMarket
- Все переменные окружения нужно будет добавить заново
- Домен нужно будет создать заново

---

## ✅ После пересоздания:

Railway будет использовать Railpack, который:
- Автоматически определит Python проект
- Найдет `requirements.txt` в папке `backend`
- Установит зависимости
- Запустит приложение



