# 🔧 Быстрое исправление проблем

## Проблема: "Internal Server Error" и пустые данные

### Шаг 1: Перезапустите бэкенд

```bash
# Остановите текущий процесс бэкенда (Ctrl+C в терминале где он запущен)
# Или найдите и убейте процесс:
lsof -i :8000
kill -9 <PID>

# Перезапустите бэкенд:
cd /Users/user/my-new-project/backend
python3 run.py
```

### Шаг 2: Очистите базу данных (если нужно)

Если проблема сохраняется, возможно нужно пересоздать базу:

```bash
cd /Users/user/my-new-project/backend
rm sql_app.db  # Удаляем старую базу
python3 -c "from app.database import engine, Base; from app import models; Base.metadata.create_all(bind=engine)"
```

### Шаг 3: Создайте пользователя вручную

Откройте новый терминал и выполните:

```bash
curl -X POST http://localhost:8000/api/users/ \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789, "username": "test_user", "first_name": "Test"}'
```

### Шаг 4: Инициализируйте тестовые задания

```bash
curl -X POST http://localhost:8000/api/admin/init-test-tasks
```

### Шаг 5: Обновите страницу в браузере

После выполнения всех шагов обновите страницу в браузере (F5 или Cmd+R).

---

## Если ничего не помогает

Проверьте логи бэкенда в терминале, где запущен `python3 run.py`. Там должны быть детали ошибки.

Также проверьте:
1. Бэкенд запущен на порту 8000
2. Frontend запущен на порту 3000 (или другом)
3. Нет конфликтов портов




