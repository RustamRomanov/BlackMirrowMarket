# 🔧 Исправление: Бот не отвечает + TON API ошибки

## Проблема 1: Бот не отвечает в Telegram

**Причина:** Бот не запущен на Railway или токен не добавлен.

### ✅ Решение:

1. **Проверьте, что токен добавлен на Railway:**
   - Railway → ваш проект → **BlackMirrowMarket** → **Variables**
   - Убедитесь, что есть: `TELEGRAM_BOT_TOKEN=8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms`

2. **Создайте отдельный сервис для бота:**
   - Railway → "+ New" → "GitHub Repo"
   - Выберите **BlackMirrowMarket**
   - **Settings** → **Source** → **Root Directory**: `backend`
   - **Settings** → **Deploy** → **Start Command**: `python3 run_bot.py`
   - **Variables** → добавьте `TELEGRAM_BOT_TOKEN`

3. **Или интегрируйте бота в основной сервис** (см. ниже)

---

## Проблема 2: TON API ошибки 404

**Причина:** TON API переменные не настроены или адрес кошелька неправильный.

### ✅ Решение:

**Вариант A: Добавьте TON переменные (если нужны TON функции)**

На Railway → **Variables** добавьте:
```
TONAPI_KEY=AGBMV6ZNTWRBHIYAAAAIMTWM3FZWBGA2IA775HKX67VIBS5JU6PEDWITLPECOMA2IWXARNQ
TON_WALLET_ADDRESS=UQCc5ORf-eL7vBXVREuwMNOWq7jOUE--06Jvz95vRQt9dXXF
TON_WALLET_SEED=ваша_сид_фраза_12_слов
```

**Вариант B: Игнорируйте ошибки (если TON функции не нужны)**

Ошибки 404 теперь не будут спамить логи после нового деплоя. Приложение будет работать без TON переменных.

---

## 🚀 Быстрое решение

### 1. Добавьте токен бота на Railway

Railway → **BlackMirrowMarket** → **Variables**:
- `TELEGRAM_BOT_TOKEN` = `8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms`

### 2. Создайте сервис для бота

1. Railway → "+ New" → "GitHub Repo" → **BlackMirrowMarket**
2. Настройте:
   - Root Directory: `backend`
   - Start Command: `python3 run_bot.py`
   - Variables: `TELEGRAM_BOT_TOKEN=8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms`

### 3. Дождитесь деплоя

Railway автоматически задеплоит бота. Проверьте логи.

---

## ✅ Проверка

1. **Бот отвечает в Telegram:**
   - Отправьте `/start` боту
   - Бот должен ответить

2. **TON API ошибки:**
   - После нового деплоя ошибки 404 не должны спамить логи
   - Приложение должно работать нормально



