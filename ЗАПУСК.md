# 🚀 Инструкция по запуску приложения

## Быстрый запуск (все сразу)

Откройте терминал и выполните:

```bash
cd /Users/user/my-new-project
./start_all.sh
```

Это запустит и бэкенд, и фронтенд одновременно.

---

## Запуск по отдельности

### 1. Запуск бэкенда

Откройте **первый терминал** и выполните:

```bash
cd /Users/user/my-new-project
./start_backend.sh
```

Или вручную:
```bash
cd /Users/user/my-new-project
source .venv/bin/activate
cd backend
python3 run.py
```

Бэкенд будет доступен на: **http://localhost:8000**

### 2. Запуск фронтенда

Откройте **второй терминал** и выполните:

```bash
cd /Users/user/my-new-project
./start_frontend.sh
```

Или вручную:
```bash
cd /Users/user/my-new-project/frontend
npm run dev
```

Фронтенд будет доступен на: **http://localhost:3000**

---

## Проверка работы

1. Откройте браузер и перейдите на **http://localhost:3000**
2. Проверьте, что бэкенд работает: **http://localhost:8000/health** (должен вернуть `{"status":"healthy"}`)

---

## Если что-то не работает

### Проблема: "ModuleNotFoundError: No module named 'uvicorn'"

Установите зависимости:
```bash
cd /Users/user/my-new-project
source .venv/bin/activate
pip install uvicorn fastapi sqlalchemy python-dotenv pydantic-settings aiohttp python-multipart
```

### Проблема: "npm: command not found"

Установите Node.js: https://nodejs.org/

### Проблема: "Port 8000 already in use"

Остановите процесс на порту 8000:
```bash
lsof -ti:8000 | xargs kill -9
```

### Проблема: "Port 3000 already in use"

Остановите процесс на порту 3000:
```bash
lsof -ti:3000 | xargs kill -9
```

### Проблема: База данных не работает

Пересоздайте базу данных:
```bash
cd /Users/user/my-new-project/backend
rm -f sql_app.db
python3 -c "from app.database import engine, Base; from app import models; Base.metadata.drop_all(bind=engine); Base.metadata.create_all(bind=engine); print('✅ База пересоздана')"
```

---

## Остановка приложения

Нажмите **Ctrl+C** в терминале(ах) где запущены серверы.

Или закройте терминалы.



