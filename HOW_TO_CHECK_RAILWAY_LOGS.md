# Как проверить логи Railway

## Пошаговая инструкция:

1. **Откройте Railway Dashboard:**
   - Перейдите на https://railway.app
   - Войдите в свой аккаунт

2. **Выберите проект:**
   - Найдите проект "BlackMirrowMarket" (или название вашего проекта)
   - Нажмите на него

3. **Выберите сервис:**
   - В списке сервисов найдите **Backend** сервис (тот, который запускает Python приложение)
   - Нажмите на него

4. **Откройте вкладку Logs:**
   - В верхнем меню найдите вкладку **"Logs"** (или "Логи")
   - Нажмите на неё
   - **ВАЖНО:** В Railway есть несколько типов логов:
     - **HTTP Logs** - только HTTP запросы (то, что вы видите сейчас)
     - **Deploy Logs** - логи при деплое
     - **Общие логи** - все print() и stderr вывод
   - Для проверки депозитов нужно смотреть **общие логи**, а не только HTTP Logs

5. **Проверьте логи:**
   - Вы увидите поток логов в реальном времени
   - Используйте поиск (Ctrl+F или Cmd+F) для поиска:
     - `8032604270` - Telegram ID пользователя
     - `✅ Автоматически зачислено` - успешное зачисление
     - `⚠️ Ошибка обработки депозита` - ошибка при обработке
     - `🔍 Проверка входящих депозитов` - начало проверки
     - `TON API error` - ошибка API
     - `Error checking deposits` - общая ошибка

## Что искать в логах:

### ✅ Хорошие сообщения:
- `🔄 Фоновая задача проверки депозитов запущена` - задача работает
- `🔍 Проверка входящих депозитов...` - проверка началась
- `📊 Найдено транзакций: X` - найдены транзакции
- `✅ Автоматически зачислено X TON пользователю 8032604270` - **успешное зачисление**

### ⚠️ Проблемные сообщения:
- `⚠️ TON сервис не настроен` - TON API не настроен
- `⚠️ TON API не настроен: api_key или wallet_address отсутствуют` - переменные окружения не установлены
- `⚠️ Telegram ID не найден в комментарии` - пользователь не указал ID в комментарии
- `⚠️ Пользователь с Telegram ID 8032604270 не найден в базе данных` - пользователь не зарегистрирован
- `❌ Error in check_deposits_periodically` - ошибка в фоновой задаче

## Фильтрация логов:

В интерфейсе Railway Logs можно:
- Использовать поиск (поле "Q Filter and search logs")
- Фильтровать по времени (выпадающий список "Last 15 min")
- Остановить/запустить автообновление (кнопка play/pause)

## Если не видите логов о проверке депозитов:

1. Проверьте, что фоновая задача запущена:
   - Ищите: `🔄 Фоновая задача проверки депозитов запущена`
   - Если не видите - перезапустите сервис

2. Проверьте переменные окружения:
   - Railway → Backend сервис → Variables
   - Должны быть установлены:
     - `TONAPI_KEY`
     - `TON_WALLET_ADDRESS`
     - `TON_WALLET_SEED` (опционально, для отправки транзакций)

3. Проверьте, что сервис работает:
   - Ищите обычные логи приложения (HTTP запросы)
   - Если их нет - сервис не запущен

