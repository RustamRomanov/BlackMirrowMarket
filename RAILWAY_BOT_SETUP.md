# 🤖 Настройка Telegram Bot на Railway

## 📋 Вариант 1: Отдельный сервис для бота (Рекомендуется)

### Шаг 1: Создайте новый сервис на Railway

1. В Railway откройте ваш проект **BlackMirrowMarket**
2. Нажмите **"+ New"** → **"GitHub Repo"**
3. Выберите репозиторий **BlackMirrowMarket**
4. Railway автоматически создаст новый сервис

### Шаг 2: Настройте сервис бота

1. Откройте созданный сервис
2. Перейдите в **Settings** → **Source**
3. Установите **Root Directory**: `backend`
4. Перейдите в **Settings** → **Deploy**
5. Установите **Start Command**: `python3 run_bot.py`

### Шаг 3: Добавьте переменные окружения

1. Перейдите в **Variables**
2. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN` = `8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms`
   - `DATABASE_URL` = `${{Postgres.DATABASE_URL}}` (если нужна БД)
   - Все остальные переменные из основного сервиса (если нужны)

### Шаг 4: Дождитесь деплоя

Railway автоматически задеплоит бота. Проверьте логи в **Deployments**.

---

## 📋 Вариант 2: Интеграция бота в основной сервис

Если хотите запускать бота вместе с FastAPI, можно интегрировать его в `main.py`.

**⚠️ Внимание:** Это может быть менее надежно, так как бот и API будут в одном процессе.

---

## 🔍 Проверка работы бота

### Локально:

```bash
cd backend
source venv/bin/activate
python3 run_bot.py
```

Вы должны увидеть:
```
🤖 Запуск Telegram бота...
✅ Токен найден: 824458186...
✅ Бот настроен и запущен!
📱 Бот готов к работе. Нажмите Ctrl+C для остановки.
```

### На Railway:

1. Откройте **Deployments** → последний деплой
2. Откройте **Deploy Logs**
3. Ищите сообщения о запуске бота

---

## 🆘 Проблемы и решения

### Бот не отвечает в Telegram

1. **Проверьте, что бот запущен:**
   - Локально: процесс `python3 run_bot.py` должен работать
   - На Railway: проверьте логи деплоя

2. **Проверьте токен:**
   - Убедитесь, что токен правильный
   - Проверьте, что токен добавлен в Variables

3. **Проверьте логи:**
   - Локально: смотрите вывод в терминале
   - На Railway: смотрите Deploy Logs

### Бот запускается, но не отвечает

1. Проверьте, что бот не заблокирован в Telegram
2. Попробуйте отправить `/start` еще раз
3. Проверьте логи на наличие ошибок

---

## ✅ Чеклист

- [ ] Токен добавлен в Railway Variables
- [ ] Сервис бота создан на Railway (или интегрирован в основной)
- [ ] Root Directory установлен: `backend`
- [ ] Start Command установлен: `python3 run_bot.py`
- [ ] Деплой успешен
- [ ] Бот отвечает на `/start` в Telegram



