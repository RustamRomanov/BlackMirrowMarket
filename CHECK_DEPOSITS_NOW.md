# Проверка депозита пользователя 8032604270 - Инструкция

## Где искать логи в Railway:

### Вариант 1: Общие логи (рекомендуется)
1. Railway → Backend сервис → вкладка **"Logs"**
2. **НЕ** используйте фильтр "HTTP Logs" - смотрите все логи
3. Ищите сообщения с эмодзи:
   - `🔄 Фоновая задача проверки депозитов запущена`
   - `🔍 Проверка входящих депозитов...`
   - `📊 Найдено транзакций: X`
   - `✅ Автоматически зачислено`

### Вариант 2: Deploy Logs
1. Railway → Backend сервис → вкладка **"Deployments"**
2. Нажмите на последний деплой
3. Откройте **"Deploy Logs"**
4. Ищите сообщения о запуске приложения

## Что проверить:

### 1. Запускается ли фоновая задача?
Ищите в логах:
```
🔄 Фоновая задача проверки депозитов запущена
```

Если не видите - фоновая задача не запустилась.

### 2. Настроен ли TON API?
Ищите в логах:
```
⚠️ TON сервис не настроен, пропускаем проверку депозитов
```
или
```
⚠️ TON API не настроен: api_key или wallet_address отсутствуют
```

Если видите - проверьте переменные окружения на Railway:
- `TONAPI_KEY`
- `TON_WALLET_ADDRESS`

### 3. Находятся ли транзакции?
Ищите в логах:
```
🔍 Проверка депозитов для кошелька: ...
📊 Найдено транзакций: X
```

### 4. Находится ли Telegram ID в комментарии?
Ищите в логах:
```
📝 Комментарий транзакции ...: ...
✅ Найден Telegram ID в комментарии: 8032604270
```
или
```
⚠️ Telegram ID не найден в комментарии
```

### 5. Зачисляется ли баланс?
Ищите в логах:
```
✅ Автоматически зачислено X TON пользователю 8032604270
```

## Если логи не видны:

1. **Проверьте, что деплой завершился:**
   - Railway → Backend сервис → Deployments
   - Последний деплой должен быть "Active"

2. **Перезапустите сервис:**
   - Railway → Backend сервис → Settings → Restart

3. **Проверьте переменные окружения:**
   - Railway → Backend сервис → Variables
   - Должны быть: `TONAPI_KEY`, `TON_WALLET_ADDRESS`

## Быстрое решение (если автоматика не работает):

Используйте админку для ручного пополнения:
1. Откройте: `https://blackmirrowmarket-production.up.railway.app/admin`
2. Перейдите в "Балансы" (User Balance)
3. Используйте форму "Пополнить баланс пользователя"
4. Введите Telegram ID: `8032604270` и сумму

