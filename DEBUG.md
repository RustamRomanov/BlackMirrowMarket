# 🔧 Отладка и исправление проблем

## ✅ Что уже работает:

1. **Backend запущен** на `http://localhost:8000`
2. **Frontend запущен** на `http://localhost:3000`
3. **Тестовые задания созданы** (20 заданий)
4. **Тестовый пользователь создан** (ID: 123456789)

## 🔍 Проверка работы:

### 1. Проверьте Backend:
```bash
curl http://localhost:8000/health
```
Должен вернуть: `{"status":"healthy"}`

### 2. Проверьте задания:
```bash
curl 'http://localhost:8000/api/tasks/?telegram_id=123456789'
```
Должен вернуть список заданий в JSON

### 3. Откройте браузер:
- Перейдите на: `http://localhost:3000`
- Откройте консоль разработчика (F12 или Cmd+Option+I)
- Проверьте ошибки в консоли

## 🐛 Частые проблемы:

### Проблема: "User not found" или пустой список заданий

**Решение:** 
1. Откройте консоль браузера
2. Проверьте, что пользователь создан автоматически
3. Если нет - заполните профиль вручную:
   - Перейдите в раздел "Профиль"
   - Заполните: Возраст (например, 25), Пол (например, male), Страна (например, Russia)
   - Нажмите "Сохранить"

### Проблема: Задания не отображаются

**Проверьте:**
1. Профиль заполнен полностью (возраст, пол, страна)
2. Баланс не отрицательный
3. В консоли браузера нет ошибок API

### Проблема: Ошибки CORS

**Решение:** Backend уже настроен на CORS, но если проблемы остаются:
- Проверьте, что backend запущен на порту 8000
- Проверьте файл `.env` в frontend: `VITE_API_URL=http://localhost:8000`

### Проблема: Порт занят

**Решение:**
```bash
# Найти процесс на порту 8000
lsof -ti:8000

# Остановить процесс
kill -9 $(lsof -ti:8000)

# Или для порта 3000
kill -9 $(lsof -ti:3000)
```

## 🧪 Режим разработки:

Приложение работает в режиме разработки:
- Автоматически создается тестовый пользователь (ID: 123456789)
- Не требуется Telegram для тестирования
- Можно работать прямо в браузере

## 📝 Логи для отладки:

### Backend логи:
- Запущенный backend показывает все запросы в консоли
- Проверьте ошибки SQL или валидации

### Frontend логи:
- Откройте консоль браузера (F12)
- Проверьте Network tab для запросов к API
- Проверьте Console для ошибок JavaScript

## ✅ Чеклист работоспособности:

- [ ] Backend отвечает на `/health`
- [ ] Backend возвращает задания на `/api/tasks/`
- [ ] Frontend открывается в браузере
- [ ] Нет ошибок в консоли браузера
- [ ] Профиль пользователя заполнен
- [ ] Задания отображаются в списке
- [ ] Можно нажать на задание и увидеть детали

## 🆘 Если ничего не помогает:

1. Перезапустите все:
   ```bash
   # Остановите все процессы
   kill -9 $(lsof -ti:8000) 2>/dev/null
   kill -9 $(lsof -ti:3000) 2>/dev/null
   
   # Запустите заново
   cd /Users/user/my-new-project/backend
   python3 run.py
   
   # В другом терминале
   cd /Users/user/my-new-project/frontend
   npm run dev
   ```

2. Проверьте базу данных:
   - Файл `blackmirrowmarket.db` должен быть в папке `backend`
   - Если его нет - создастся автоматически при первом запуске

3. Очистите кеш браузера:
   - Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)




