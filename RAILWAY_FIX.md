# 🔧 Исправление проблемы с Dockerfile в Railway

## Проблема

Railway не может найти Dockerfile и выдает ошибку: `Dockerfile does not exist`

## Решение

Используем **Nixpacks** (автоматическое определение) вместо Dockerfile. Это проще и надежнее.

## Что нужно сделать в Railway:

### 1. Установить Root Directory

1. Откройте **Settings** → раздел **Source**
2. Установите **Root Directory:** `backend`
3. Сохраните

### 2. Настроить Build

1. Откройте **Settings** → раздел **Build**
2. Убедитесь, что выбран **"Nixpacks"** (не Dockerfile)
3. Если выбран Dockerfile - переключите на Nixpacks

### 3. Настроить Deploy

1. Откройте **Settings** → раздел **Deploy**
2. Установите **Start Command:**
   ```
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

### 4. Переменные окружения

Убедитесь, что все переменные добавлены в разделе **Variables**

---

## Почему Nixpacks лучше?

- Автоматически определяет Python проект
- Находит `requirements.txt` и устанавливает зависимости
- Не требует Dockerfile
- Проще в настройке

---

## После настройки

Railway автоматически начнет новый деплой. Дождитесь завершения (статус станет зеленым).

