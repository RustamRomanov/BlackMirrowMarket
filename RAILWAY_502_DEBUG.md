# 🔍 Отладка ошибки 502: Application failed to respond

## Проблема

Приложение возвращает 502 ошибку - это значит, что приложение не запускается или падает сразу после старта.

## Что нужно проверить

### 1. Проверьте логи деплоя

В Railway:
1. Откройте сервис **BlackMirrowMarket**
2. Перейдите в **Deployments**
3. Нажмите на последний деплой
4. Откройте вкладку **"Deploy Logs"** (не Build Logs!)
5. Прокрутите вниз и найдите ошибки

### 2. Возможные причины

#### A. Ошибка при импорте модулей
- Может быть ошибка типа `ModuleNotFoundError`
- Или ошибка синтаксиса при импорте

#### B. Ошибка подключения к базе данных
- `DATABASE_URL` не настроен
- База данных недоступна
- Неправильный формат `DATABASE_URL`

#### C. Отсутствуют переменные окружения
- `SECRET_KEY` не установлен
- Другие обязательные переменные отсутствуют

#### D. Ошибка при запуске сервера
- Неправильный Start Command
- Порт не слушается

### 3. Что проверить в настройках

1. **Start Command:**
   - Settings → Deploy → Start Command
   - Должно быть: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

2. **Variables:**
   - Убедитесь, что все переменные добавлены
   - Особенно: `DATABASE_URL`, `SECRET_KEY`

3. **Root Directory:**
   - Settings → Source → Root Directory = `backend`

---

## 🔧 Быстрое решение

### Проверьте логи и найдите ошибку

Скопируйте последние строки из **Deploy Logs** и отправьте мне - я помогу исправить!

---

## 📝 Частые ошибки и решения

### Ошибка: "ModuleNotFoundError"
**Решение:** Проверьте, что все зависимости установлены в `requirements.txt`

### Ошибка: "Database connection error"
**Решение:** 
1. Проверьте, что `DATABASE_URL = ${{Postgres.DATABASE_URL}}`
2. Убедитесь, что Postgres сервис запущен

### Ошибка: "SECRET_KEY not set"
**Решение:** Добавьте переменную `SECRET_KEY` в Variables

### Ошибка: "Port already in use"
**Решение:** Убедитесь, что Start Command использует `$PORT`

---

## ✅ После исправления

1. Railway автоматически перезапустит сервис
2. Проверьте: https://blackmirrowmarket-production.up.railway.app/health
3. Должно вернуть: `{"status":"healthy"}`



