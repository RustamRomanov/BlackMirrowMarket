# 🤖 Настройка Telegram Bot для BlackMirrowMarket

## 📋 Шаг 1: Создание бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: `BlackMirrowMarket Bot`)
4. Введите username бота (должен заканчиваться на `bot`, например: `blackmirrowmarket_bot`)
5. BotFather отправит вам **токен бота** - сохраните его!

**Пример токена:** `8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms`

---

## 📋 Шаг 2: Настройка Mini App

1. В BotFather отправьте команду `/newapp`
2. Выберите вашего бота из списка
3. Введите название Mini App (например: `BlackMirrowMarket`)
4. Введите описание (например: `Маркетплейс микро-задач`)
5. Загрузите иконку (опционально)
6. Введите **Web App URL**: `https://ваш-frontend-домен.railway.app`
   - Если фронтенд еще не развернут, можно использовать: `https://blackmirrowmarket-production.up.railway.app`
   - Позже можно изменить через `/myapps`

---

## 📋 Шаг 3: Добавление переменных окружения в Railway

1. Откройте Railway → ваш проект → сервис **BlackMirrowMarket**
2. Перейдите в **Variables**
3. Добавьте следующие переменные:

### Обязательные переменные:

```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

**Пример:**
```
TELEGRAM_BOT_TOKEN=8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms
```

### Опциональные переменные:

```
TELEGRAM_ADMIN_BOT_TOKEN=токен_админ_бота_если_есть
```

---

## 📋 Шаг 4: Запуск бота

Есть два способа запуска бота:

### Вариант A: Polling (проще, но требует отдельный процесс)

Бот будет работать через polling (постоянно опрашивает Telegram API).

**Для локального запуска:**
```bash
cd backend
source venv/bin/activate
python3 app/telegram_bot.py
```

**Для Railway:**
Нужно создать отдельный сервис или запустить бота в фоне вместе с FastAPI.

### Вариант B: Webhook (рекомендуется для production)

Бот будет получать обновления через webhook (более эффективно).

**Настройка webhook:**
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook?url=https://blackmirrowmarket-production.up.railway.app/webhook/telegram"
```

---

## 📋 Шаг 5: Проверка работы бота

1. Найдите вашего бота в Telegram (по username, который вы указали)
2. Отправьте команду `/start`
3. Бот должен ответить: "Добро пожаловать в BlackMirrowMarket!"

---

## 🔧 Интеграция бота в FastAPI (опционально)

Если хотите запускать бота вместе с FastAPI, можно добавить в `main.py`:

```python
from app.telegram_bot import setup_bot
import asyncio

@app.on_event("startup")
async def startup_event():
    """Запускаем фоновые задачи при старте приложения."""
    # ... существующий код ...
    
    # Запускаем Telegram бота в фоне
    bot_token = os.getenv("TELEGRAM_BOT_TOKEN")
    if bot_token:
        bot_app = setup_bot(bot_token)
        asyncio.create_task(bot_app.initialize())
        asyncio.create_task(bot_app.start())
        asyncio.create_task(bot_app.updater.start_polling())
```

---

## 📝 Текущие команды бота

- `/start` - Приветствие и начало работы
- `/help` - Справка по командам

---

## 🚀 Следующие шаги

1. ✅ Добавьте `TELEGRAM_BOT_TOKEN` в Railway Variables
2. ✅ Настройте Mini App через BotFather
3. ✅ Запустите бота (локально или на Railway)
4. ✅ Протестируйте команды `/start` и `/help`

---

## ⚠️ Важные замечания

1. **Безопасность:** Никогда не публикуйте токен бота в открытом доступе
2. **Polling vs Webhook:** 
   - Polling проще для разработки
   - Webhook эффективнее для production
3. **Отдельный сервис:** Для production лучше запускать бота в отдельном сервисе на Railway

---

## 🆘 Проблемы и решения

### Бот не отвечает
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильно установлен
- Убедитесь, что бот запущен
- Проверьте логи в Railway

### Mini App не открывается
- Проверьте, что URL правильный и доступен
- Убедитесь, что фронтенд развернут и работает
- Проверьте CORS настройки

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [python-telegram-bot Documentation](https://python-telegram-bot.org/)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)

