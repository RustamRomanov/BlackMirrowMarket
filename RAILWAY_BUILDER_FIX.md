# 🔧 Правильная настройка Builder в Railway

## ❌ Что НЕ нужно делать:

1. **НЕ вводите "Nixpacks" в поле "Dockerfile Path"** - это поле только для Dockerfile!
2. **НЕ выбирайте "Dockerfile" в Builder** - если у вас нет Dockerfile в корне

## ✅ Правильная настройка:

### Вариант 1: Railpack (РЕКОМЕНДУЕТСЯ - новый по умолчанию)

1. В разделе **"Builder"** выберите **"Railpack"** (с тегом "Default")
2. **Оставьте поле "Dockerfile Path" ПУСТЫМ** (или удалите "Nixpacks" оттуда)
3. Railpack автоматически определит Python проект и соберет его

### Вариант 2: Nixpacks (работает, но deprecated)

1. В разделе **"Builder"** выберите **"Nixpacks"** (помечен как "Deprecated")
2. **Оставьте поле "Dockerfile Path" ПУСТЫМ** (удалите "Nixpacks" оттуда)
3. Nixpacks автоматически определит Python проект

## 📝 Важно:

- **"Dockerfile Path"** - это поле ТОЛЬКО для Builder = "Dockerfile"
- Если выбран Railpack или Nixpacks, это поле **НЕ НУЖНО**
- **Удалите "Nixpacks" из поля "Dockerfile Path"** - оставьте его пустым!

## 🎯 Рекомендация:

Используйте **Railpack** (новый по умолчанию) - он лучше и быстрее.

---

## Пошаговая инструкция:

1. В разделе **"Builder"** выберите **"Railpack"**
2. В поле **"Dockerfile Path"** удалите "Nixpacks" (оставьте пустым)
3. Убедитесь, что **Root Directory = `backend`** (в разделе Source)
4. Сохраните изменения
5. Railway автоматически начнет новый деплой

