# 🔧 Исправление ошибки 502: Application failed to respond

## Проблема

Деплой успешен, но приложение возвращает 502 ошибку. Это значит, что сборка прошла, но приложение не запустилось или упало.

## Решение

### Шаг 1: Проверьте логи

1. В Railway откройте сервис **BlackMirrowMarket**
2. Перейдите в **Deployments**
3. Нажмите на последний успешный деплой
4. Нажмите **"View logs"**
5. Проверьте логи на наличие ошибок

### Шаг 2: Возможные причины

#### 1. Приложение не запускается

**Проверьте Start Command:**
- Settings → Deploy → Start Command
- Должно быть: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

#### 2. Отсутствуют переменные окружения

**Проверьте Variables:**
- Убедитесь, что все переменные добавлены
- Особенно важно: `DATABASE_URL`, `SECRET_KEY`

#### 3. Ошибка при импорте модулей

**Проверьте логи:**
- Могут быть ошибки типа "ModuleNotFoundError"
- Или ошибки подключения к базе данных

#### 4. База данных не инициализирована

**Инициализируйте БД:**
```bash
railway run --service BlackMirrowMarket python3 backend/init_db.py
```

### Шаг 3: Проверьте настройки

1. **Root Directory:**
   - Settings → Source → Root Directory = `backend`

2. **Start Command:**
   - Settings → Deploy → Start Command = `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

3. **Variables:**
   - Убедитесь, что все переменные добавлены

### Шаг 4: Перезапустите сервис

1. В Railway найдите сервис BlackMirrowMarket
2. Нажмите на **три точки (⋮)**
3. Выберите **"Restart"** или **"Redeploy"**

---

## 🔍 Что проверить в логах

Ищите ошибки типа:
- `ModuleNotFoundError`
- `ImportError`
- `Database connection error`
- `Port already in use`
- `Permission denied`

---

## ✅ После исправления

После исправления ошибок:
1. Railway автоматически перезапустит сервис
2. Проверьте: https://blackmirrowmarket-production.up.railway.app/health
3. Должно вернуть: `{"status":"healthy"}`

