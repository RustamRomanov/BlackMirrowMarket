# 🚂 Railway - Пошаговая инструкция с картинками

## ✅ Шаг 1: Проверка GitHub (УЖЕ СДЕЛАНО!)

Ваш репозиторий: `RustamRomanov/BlackMirrowMarket` ✅
Код загружен на GitHub ✅

---

## 🚀 Шаг 2: Создание проекта в Railway

1. Откройте: https://railway.app
2. Нажмите **"New Project"** (кнопка вверху справа)
3. Выберите **"Deploy from GitHub repo"**
4. Найдите и выберите репозиторий **`RustamRomanov/BlackMirrowMarket`**
5. Railway создаст проект и начнет автоматический деплой

**⚠️ ВАЖНО:** Этот первый деплой скорее всего упадет с ошибкой - это нормально! Нужно настроить сервис.

---

## 🗄️ Шаг 3: Добавление PostgreSQL

1. В вашем проекте Railway нажмите **"+ New"** (кнопка слева)
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway создаст базу данных (это займет 1-2 минуты)
4. **Готово!** Railway автоматически создаст переменную `DATABASE_URL`

---

## ⚙️ Шаг 4: Настройка Backend сервиса

### 4.1 Где найти "Configure Service"

После создания проекта Railway автоматически создает сервис из вашего репозитория. 

**Как найти настройки:**
1. В проекте Railway найдите карточку с названием вашего репозитория (например, "BlackMirrowMarket")
2. **Нажмите на эту карточку** - откроется страница сервиса
3. Вверху страницы есть вкладки: **"Deployments"**, **"Variables"**, **"Metrics"**, **"Settings"**
4. Нажмите на вкладку **"Settings"**

### 4.2 Настройка Root Directory и команд

В разделе **"Settings"** найдите секцию **"Service"**:

1. **Root Directory:** 
   - Введите: `backend`
   - Это говорит Railway, что код находится в папке `backend`

2. **Build Command:**
   - Введите: `pip install -r requirements.txt`
   - Это команда для установки зависимостей

3. **Start Command:**
   - Введите: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
   - Это команда для запуска сервера

4. **Нажмите "Save"** или изменения сохранятся автоматически

### 4.3 Добавление переменных окружения

1. Перейдите на вкладку **"Variables"** (рядом с Settings)
2. Нажмите **"+ New Variable"** для каждой переменной
3. Добавьте следующие переменные:

```
DATABASE_URL = ${{Postgres.DATABASE_URL}}
```

```
TON_WALLET_ADDRESS = UQCc5ORf-eL7vBXVREuwMNOWq7jOUE--06Jvz95vRQt9dXXF
```

```
TON_WALLET_SEED = "ваша_seed_фраза_12_слов_в_кавычках"
```

```
TONAPI_KEY = AGBMV6ZNTWRBHIYAAAAIMTWM3FZWBGA2IA775HKX67VIBS5JU6PEDWITLPECOMA2IWXARNQ
```

```
SECRET_KEY = zzNrqQslV2PkB9AVmpNfN4vOGAhbyWUmDglSfVRASpE
```

```
ADMIN_USERNAME = admin
```

```
ADMIN_PASSWORD = ваш_надежный_пароль
```

```
ENVIRONMENT = production
```

```
DEBUG = False
```

```
CORS_ORIGINS = https://t.me,https://web.telegram.org
```

**Важно:** 
- Для `DATABASE_URL` используйте синтаксис `${{Postgres.DATABASE_URL}}` - Railway автоматически подставит значение
- Для `TON_WALLET_SEED` используйте кавычки, если там пробелы

### 4.4 Создание домена

1. В **"Settings"** найдите секцию **"Domains"**
2. Нажмите **"Generate Domain"**
3. Railway создаст домен типа: `blackmirrowmarket-production-xxxx.up.railway.app`
4. **Скопируйте и сохраните этот домен!** Он понадобится для Frontend

### 4.5 Перезапуск деплоя

После настройки:
1. Перейдите на вкладку **"Deployments"**
2. Railway автоматически начнет новый деплой с правильными настройками
3. Или нажмите **"Redeploy"** (если есть такая кнопка)

---

## 🎨 Шаг 5: Настройка Frontend сервиса

### 5.1 Создание нового сервиса

1. В проекте Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий **`RustamRomanov/BlackMirrowMarket`**
3. Railway создаст новый сервис

### 5.2 Настройка Frontend

1. Нажмите на карточку нового сервиса
2. Перейдите в **"Settings"**
3. Настройте:

   - **Root Directory:** `frontend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx serve -s dist -l $PORT`

4. Сохраните

### 5.3 Переменные окружения Frontend

1. Перейдите в **"Variables"**
2. Добавьте:

```
VITE_API_URL = https://ваш-backend-домен.railway.app
```

**Важно:** Замените `ваш-backend-домен` на реальный домен из шага 4.4!

### 5.4 Создание домена Frontend

1. В **"Settings"** → **"Domains"** → **"Generate Domain"**
2. Сохраните домен Frontend

### 5.5 Обновление CORS в Backend

Вернитесь в Backend сервис → **"Variables"** и обновите:

```
CORS_ORIGINS = https://t.me,https://web.telegram.org,https://ваш-frontend-домен.railway.app
```

---

## 🗄️ Шаг 6: Инициализация базы данных

1. В Backend сервисе откройте вкладку **"Deployments"**
2. Дождитесь успешного деплоя (зеленый статус)
3. Откройте вкладку **"Logs"** или найдите кнопку **"Shell"**
4. В Shell выполните:

```bash
cd backend
python3 init_db.py
```

Или через Railway CLI (если установлен):
```bash
railway run --service backend python3 backend/init_db.py
```

---

## ✅ Шаг 7: Проверка

### Backend:
- Откройте: `https://ваш-backend-домен.railway.app/health`
- Должно вернуть: `{"status":"healthy"}`

### Frontend:
- Откройте: `https://ваш-frontend-домен.railway.app`
- Должен открыться интерфейс

---

## 🆘 Решение проблем

### Ошибка "There was an error deploying from source"

**Причина:** Railway пытается задеплоить весь репозиторий без настроек.

**Решение:**
1. Перейдите в **Settings** сервиса
2. Убедитесь, что указан **Root Directory: `backend`**
3. Убедитесь, что указаны **Build Command** и **Start Command**
4. Сохраните и дождитесь нового деплоя

### Не могу найти "Configure Service"

**Где искать:**
1. Нажмите на карточку сервиса (с названием репозитория)
2. Вверху страницы будут вкладки: **Deployments**, **Variables**, **Metrics**, **Settings**
3. Нажмите **"Settings"** - там все настройки

### Сервис не запускается

1. Проверьте **Logs** в разделе **Deployments**
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что `DATABASE_URL` правильный
4. Убедитесь, что `SECRET_KEY` установлен

---

## 📸 Где что находится

```
Railway Dashboard
├── Проект (hopeful-perception)
    ├── Сервис 1 (Backend - BlackMirrowMarket)
    │   ├── Deployments (вкладка)
    │   ├── Variables (вкладка) ← здесь переменные
    │   ├── Metrics (вкладка)
    │   └── Settings (вкладка) ← здесь Root Directory, Build, Start
    ├── Сервис 2 (Frontend - BlackMirrowMarket)
    │   └── (те же вкладки)
    └── PostgreSQL (база данных)
```

---

## 🎯 Краткий чеклист

- [ ] Проект создан в Railway
- [ ] PostgreSQL добавлен
- [ ] Backend сервис настроен (Root Directory: `backend`)
- [ ] Backend переменные добавлены
- [ ] Backend домен создан
- [ ] Frontend сервис создан
- [ ] Frontend настроен (Root Directory: `frontend`)
- [ ] Frontend переменные добавлены (VITE_API_URL)
- [ ] Frontend домен создан
- [ ] CORS обновлен в Backend
- [ ] База данных инициализирована
- [ ] Все работает!

---

## 💡 Совет

Если что-то не работает:
1. Проверьте **Logs** в разделе **Deployments**
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что Root Directory правильный
4. Убедитесь, что команды Build и Start правильные

