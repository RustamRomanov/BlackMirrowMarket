# 🚂 Railway - Простые шаги (без путаницы)

## ✅ Что нужно сделать в Railway:

### 1. Раздел "Source" (боковая панель справа)

1. Нажмите на **"Source"** в боковой панели справа
2. Найдите **"Add Root Directory"** или поле "Root Directory"
3. Введите: `backend`
4. Сохраните (обычно сохраняется автоматически)

---

### 2. Раздел "Build" (боковая панель справа)

1. Нажмите на **"Build"** в боковой панели справа
2. Убедитесь, что выбран **"Dockerfile"** (должен быть выбран автоматически)
3. Проверьте **"Dockerfile Path"**:
   - Если Root Directory = `backend`, то путь должен быть: `Dockerfile`
   - Или оставьте как есть: `/backend/Dockerfile`
4. **НЕ ИЩИТЕ "Build Command"** - его нет, потому что используется Dockerfile!

---

### 3. Раздел "Deploy" (боковая панель справа)

1. Нажмите на **"Deploy"** в боковой панели справа
2. Найдите поле **"Start Command"** или **"Run Command"**
3. Введите:
   ```
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```
4. Сохраните

---

### 4. Переменные окружения

1. Вверху страницы нажмите на вкладку **"Variables"**
2. Добавьте все переменные (см. список ниже)

---

### 5. Проверка

1. После сохранения всех настроек Railway автоматически начнет новый деплой
2. Перейдите на вкладку **"Deployments"**
3. Дождитесь завершения (статус станет зеленым)

---

## 📝 Переменные окружения (вкладка "Variables")

Добавьте эти переменные:

```
DATABASE_URL = ${{Postgres.DATABASE_URL}}
TON_WALLET_ADDRESS = UQCc5ORf-eL7vBXVREuwMNOWq7jOUE--06Jvz95vRQt9dXXF
TON_WALLET_SEED = "ваша_seed_фраза_12_слов"
TONAPI_KEY = AGBMV6ZNTWRBHIYAAAAIMTWM3FZWBGA2IA775HKX67VIBS5JU6PEDWITLPECOMA2IWXARNQ
SECRET_KEY = zzNrqQslV2PkB9AVmpNfN4vOGAhbyWUmDglSfVRASpE
ADMIN_USERNAME = admin
ADMIN_PASSWORD = ваш_пароль
ENVIRONMENT = production
DEBUG = False
CORS_ORIGINS = https://t.me,https://web.telegram.org
```

---

## 🎯 Главное:

- **Root Directory** = `backend` (в разделе Source)
- **Dockerfile** уже выбран (в разделе Build) ✅
- **Start Command** = `uvicorn app.main:app --host 0.0.0.0 --port $PORT` (в разделе Deploy)
- **Build Command НЕ НУЖЕН** - используется Dockerfile!

---

## ❓ Если что-то не работает:

1. Убедитесь, что Root Directory = `backend`
2. Проверьте логи в "Deployments" → выберите последний деплой → "View Logs"
3. Убедитесь, что все переменные окружения добавлены

