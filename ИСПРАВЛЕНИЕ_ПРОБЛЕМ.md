# 🔧 Инструкция по исправлению проблем

## Проблема: "Internal Server Error" и пустые данные

### Что нужно сделать:

#### 1. Проверьте логи бэкенда

Откройте терминал, где запущен бэкенд (`python3 run.py`), и посмотрите на последние ошибки. Там должна быть детальная информация об ошибке.

#### 2. Перезапустите бэкенд

```bash
# В терминале где запущен бэкенд нажмите Ctrl+C
# Затем запустите заново:
cd /Users/user/my-new-project/backend
python3 run.py
```

#### 3. Очистите базу данных (если нужно)

Если проблема сохраняется, возможно нужно пересоздать базу:

```bash
cd /Users/user/my-new-project/backend
rm sql_app.db
python3 -c "from app.database import engine, Base; from app import models; Base.metadata.create_all(bind=engine)"
```

#### 4. Создайте пользователя вручную

Откройте новый терминал и выполните:

```bash
curl -X POST http://localhost:8000/api/users/ \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": 123456789, "username": "test_user", "first_name": "Test"}'
```

Если получите ошибку, скопируйте полный текст ошибки и отправьте мне.

#### 5. Инициализируйте тестовые задания

```bash
curl -X POST http://localhost:8000/api/admin/init-test-tasks
```

#### 6. Обновите страницу в браузере

После выполнения всех шагов обновите страницу в браузере (F5 или Cmd+R).

---

## Быстрый скрипт для проверки

Я создал скрипт `fix_and_test.sh` в корне проекта. Запустите его:

```bash
cd /Users/user/my-new-project
./fix_and_test.sh
```

---

## Если ничего не помогает

**Пожалуйста, скопируйте и отправьте мне:**

1. **Логи бэкенда** - последние 20-30 строк из терминала где запущен `python3 run.py`
2. **Результат команды:**
   ```bash
   curl -v -X POST http://localhost:8000/api/users/ \
     -H "Content-Type: application/json" \
     -d '{"telegram_id": 123456789, "username": "test_user", "first_name": "Test"}'
   ```
3. **Результат команды:**
   ```bash
   curl -v http://localhost:8000/api/users/123456789
   ```

Это поможет мне точно определить проблему и исправить её.




