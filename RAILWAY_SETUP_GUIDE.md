# 🚂 Пошаговая инструкция: Настройка Railway

## 📋 Быстрая настройка (15 минут)

### ✅ Шаг 1: Регистрация на Railway

1. Откройте: https://railway.app
2. Нажмите **"Login"** → **"Login with GitHub"**
3. Разрешите доступ к вашему GitHub аккаунту

---

### ✅ Шаг 2: Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите репозиторий **`BlackMirrowMarket`**
4. Railway автоматически начнет деплой (пока остановите его, нужно настроить)

---

### ✅ Шаг 3: Добавление PostgreSQL

1. В проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway создаст базу данных
4. **Важно:** Railway автоматически создаст переменную `DATABASE_URL` для всех сервисов

---

### ✅ Шаг 4: Настройка Backend сервиса

#### 4.1 Создание сервиса

1. В проекте нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите репозиторий **`BlackMirrowMarket`**
3. Railway спросит **"Configure Service"** - нажмите на сервис
4. В настройках укажите:
   - **Root Directory:** `backend`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

#### 4.2 Переменные окружения

Перейдите в **"Variables"** и добавьте следующие переменные:

```env
# База данных (Railway автоматически создаст, но можно явно указать)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# TON настройки (замените на ваши значения)
TON_WALLET_ADDRESS=UQCc5ORf-eL7vBXVREuwMNOWq7jOUE--06Jvz95vRQt9dXXF
TON_WALLET_SEED=ваша_seed_фраза_12_слов_в_кавычках
TONAPI_KEY=AGBMV6ZNTWRBHIYAAAAIMTWM3FZWBGA2IA775HKX67VIBS5JU6PEDWITLPECOMA2IWXARNQ

# Безопасность (ВАЖНО: сгенерируйте свой SECRET_KEY!)
SECRET_KEY=ВАШ_СГЕНЕРИРОВАННЫЙ_СЕКРЕТНЫЙ_КЛЮЧ
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_надежный_пароль_админа

# Окружение
ENVIRONMENT=production
DEBUG=False

# CORS (обновите после создания домена Frontend)
CORS_ORIGINS=https://t.me,https://web.telegram.org
```

**Как сгенерировать SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

#### 4.3 Создание домена

1. В настройках Backend сервиса → **"Settings"**
2. Найдите **"Domains"**
3. Нажмите **"Generate Domain"**
4. Railway создаст домен типа: `blackmirrowmarket-production-xxxx.up.railway.app`
5. **Скопируйте и сохраните этот домен!** Он понадобится для Frontend

---

### ✅ Шаг 5: Инициализация базы данных

После первого успешного деплоя Backend:

1. В Backend сервисе откройте **"Deployments"**
2. Нажмите на последний деплой → **"View Logs"** (проверьте, что нет ошибок)
3. Откройте **"Shell"** в Railway (или используйте Railway CLI)

**Выполните:**
```bash
cd backend
python3 init_db.py
```

Или через Railway CLI:
```bash
railway run --service backend python3 backend/init_db.py
```

---

### ✅ Шаг 6: Настройка Frontend сервиса

#### 6.1 Создание сервиса

1. В проекте нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите репозиторий **`BlackMirrowMarket`**
3. В настройках укажите:
   - **Root Directory:** `frontend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx serve -s dist -l $PORT`

#### 6.2 Переменные окружения

В **"Variables"** добавьте:

```env
# API URL (замените на домен вашего Backend из шага 4.3)
VITE_API_URL=https://ваш-backend-домен.railway.app
```

**Важно:** Используйте домен Backend, который вы получили на шаге 4.3!

#### 6.3 Обновление CORS в Backend

Вернитесь в Backend сервис → **"Variables"** и обновите:

```env
CORS_ORIGINS=https://t.me,https://web.telegram.org,https://ваш-frontend-домен.railway.app
```

#### 6.4 Создание домена Frontend

1. В настройках Frontend сервиса → **"Settings"**
2. Найдите **"Domains"**
3. Нажмите **"Generate Domain"**
4. Railway создаст домен типа: `blackmirrowmarket-frontend-xxxx.up.railway.app`
5. **Скопируйте и сохраните этот домен!** Он понадобится для Telegram Bot

---

### ✅ Шаг 7: Настройка Telegram Bot

1. Откройте Telegram и напишите **@BotFather**
2. Отправьте команду `/newapp`
3. Выберите вашего бота
4. Заполните:
   - **Web App URL:** `https://ваш-frontend-домен.railway.app`
   - (остальные поля опционально)
5. Сохраните изменения

---

### ✅ Шаг 8: Проверка

#### Backend:
- Откройте: `https://ваш-backend-домен.railway.app/health`
- Должно вернуть: `{"status":"healthy"}`
- Админка: `https://ваш-backend-домен.railway.app/admin`
- Логин: `admin` / Пароль: (ваш пароль из переменных)

#### Frontend:
- Откройте: `https://ваш-frontend-домен.railway.app`
- Должен открыться интерфейс приложения

#### Telegram:
- Откройте вашего бота в Telegram
- Нажмите кнопку **"Open App"** или отправьте `/start`
- Должно открыться приложение

---

## 🔄 Автоматический деплой

После настройки, каждый `git push` в GitHub автоматически задеплоит изменения на Railway!

---

## 📊 Мониторинг

- **Логи:** Railway → Сервис → Deployments → Logs
- **Метрики:** Railway → Сервис → Metrics
- **Домены:** Railway → Сервис → Settings → Domains

---

## 🆘 Решение проблем

### Backend не запускается:
1. Проверьте логи в Railway → Deployments → Logs
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `DATABASE_URL` правильный
4. Убедитесь, что `SECRET_KEY` установлен

### Frontend не открывается:
1. Проверьте, что `VITE_API_URL` правильный (домен Backend)
2. Проверьте логи сборки в Railway
3. Убедитесь, что `npm run build` успешно выполнился

### База данных:
1. Убедитесь, что PostgreSQL сервис запущен
2. Проверьте, что `init_db.py` выполнился
3. Проверьте логи PostgreSQL в Railway

### CORS ошибки:
1. Убедитесь, что домен Frontend добавлен в `CORS_ORIGINS` Backend
2. Проверьте, что домены правильные (с https://)

---

## 💰 Стоимость

- **Бесплатно:** $5 кредитов/месяц (для тестирования)
- **Hobby:** $5/месяц + использование
- **Pro:** $20/месяц + использование

Начните с бесплатного тарифа!

---

## ✅ Чеклист

Используйте [RAILWAY_CHECKLIST.md](./RAILWAY_CHECKLIST.md) для отслеживания прогресса.

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет развернуто на Railway и готово к использованию!

