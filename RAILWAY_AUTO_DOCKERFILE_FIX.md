# 🔧 Исправление: Railway автоматически выбирает Dockerfile

## Проблема

Railway автоматически обнаружил Dockerfile и выбрал его как builder, но деплои падают с ошибкой "Dockerfile does not exist".

## Решение

### Вариант 1: Переключиться на Railpack (РЕКОМЕНДУЕТСЯ)

1. В разделе **"Builder"** нажмите на выпадающий список
2. Выберите **"Railpack"** (с тегом "Default")
3. Это переопределит автоматическое определение
4. **Убедитесь, что Root Directory = `backend`** (в разделе Source)

### Вариант 2: Использовать Dockerfile (если Root Directory установлен)

Если вы хотите использовать Dockerfile:

1. Убедитесь, что **Root Directory = `backend`** (в разделе Source)
2. В разделе **"Dockerfile Path"** должно быть: `Dockerfile` (не `/backend/Dockerfile`)
3. Потому что Root Directory уже указывает на `backend`, поэтому путь относительный

## 🎯 Рекомендация: Использовать Railpack

Railpack проще и надежнее:
- Автоматически определяет Python проект
- Находит requirements.txt
- Не требует Dockerfile
- Быстрее собирается

## Пошаговая инструкция:

1. **Установите Root Directory:**
   - Settings → Source → Root Directory = `backend`

2. **Переключите Builder:**
   - Settings → Build → Builder → выберите **"Railpack"**
   - Это переопределит автоматическое определение Dockerfile

3. **Очистите Dockerfile Path:**
   - Если там что-то есть, удалите (оставьте пустым)

4. **Проверьте Start Command:**
   - Settings → Deploy → Start Command = `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

5. **Сохраните и дождитесь деплоя**



