# 🚀 Быстрый старт - пошаговая инструкция

## Шаг 1: Откройте терминал

Откройте **один** терминал на вашем Mac.

## Шаг 2: Перейдите в проект

Скопируйте и выполните эту команду:

```bash
cd /Users/user/my-new-project
```

## Шаг 3: Настройте Backend

### 3.1. Перейдите в папку backend:

```bash
cd backend
```

### 3.2. Создайте файл .env:

Создайте файл `.env` в папке `backend` со следующим содержимым:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/blackmirrowmarket
TELEGRAM_BOT_TOKEN=8244581866:AAEayTkPVApVrITpEupY1mk7clo4q3Ncpms
TELEGRAM_ADMIN_BOT_TOKEN=8454417837:AAG8QQEqVGuHJJavlY4G84L9JM9JEWcZ8kg
SECRET_KEY=your-secret-key-here
```

**Важно:** Если у вас нет PostgreSQL, используйте SQLite для тестирования (см. ниже).

### 3.3. Установите зависимости (если еще не установлены):

```bash
python3 -m pip install -r requirements.txt
```

### 3.4. Запустите сервер:

```bash
python3 run.py
```

Сервер запустится на `http://localhost:8000`

**Оставьте этот терминал открытым!** Сервер должен работать.

## Шаг 4: Откройте ВТОРОЙ терминал

Откройте **новый** терминал (не закрывайте первый!).

### 4.1. Инициализируйте тестовые задания:

```bash
curl -X POST http://localhost:8000/api/admin/init-test-tasks
```

Вы должны увидеть ответ: `{"message":"Created X test tasks",...}`

## Шаг 5: Настройте Frontend

### 5.1. Откройте ТРЕТИЙ терминал (или используйте второй)

```bash
cd /Users/user/my-new-project/frontend
```

### 5.2. Установите зависимости:

```bash
npm install
```

### 5.3. Создайте файл .env:

```bash
echo "VITE_API_URL=http://localhost:8000" > .env
```

### 5.4. Запустите frontend:

```bash
npm run dev
```

Frontend запустится на `http://localhost:3000`

## Шаг 6: Проверьте работу

1. Откройте браузер: `http://localhost:8000/docs` - должна открыться документация API
2. Откройте: `http://localhost:3000` - должен открыться frontend

## ⚠️ Если нет PostgreSQL (использование SQLite)

Если у вас нет PostgreSQL, можно временно использовать SQLite:

1. В файле `backend/app/database.py` замените:
   ```python
   DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./blackmirrowmarket.db")
   ```

2. В файле `.env` можно не указывать DATABASE_URL (будет использован SQLite)

## 📝 Итого у вас должно быть:

- **Терминал 1:** Backend сервер работает (`python3 run.py`)
- **Терминал 2:** Frontend сервер работает (`npm run dev`)
- **Браузер:** Открыт `http://localhost:3000`

## 🆘 Если что-то не работает:

1. Проверьте, что все терминалы открыты и команды выполняются
2. Проверьте, что порты 8000 и 3000 свободны
3. Убедитесь, что зависимости установлены
