# 🔧 Финальное исправление: Railway автоматически определяет Dockerfile

## Проблема

Railway автоматически обнаружил Dockerfile и не дает его изменить в Builder. Деплои падают с ошибкой "Dockerfile does not exist".

## Решение

### Что было сделано:

1. **Dockerfile переименован** в `Dockerfile.backup` - Railway больше не будет его автоматически определять
2. Теперь Railway будет использовать **Railpack** (автоматическое определение Python проекта)

### Что нужно сделать в Railway:

1. **Проверьте Root Directory:**
   - Settings → Source → Root Directory
   - Должно быть: `backend` (БЕЗ слеша в начале!)
   - Если там `/backend` - измените на `backend`

2. **Проверьте Builder:**
   - Settings → Build → Builder
   - Теперь должен быть выбран **"Railpack"** (или "Nixpacks")
   - Если все еще "Dockerfile" - подождите, Railway обновит после нового деплоя

3. **Проверьте Dockerfile Path:**
   - Settings → Build → Dockerfile Path
   - Должно быть **ПУСТО** (если используется Railpack)

4. **Проверьте Start Command:**
   - Settings → Deploy → Start Command
   - Должно быть: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

5. **Дождитесь нового деплоя:**
   - Railway автоматически начнет новый деплой
   - Проверьте вкладку "Deployments"
   - Должен пройти успешно!

---

## ⚠️ Важно про Root Directory:

- **Неправильно:** `/backend` (со слешем)
- **Правильно:** `backend` (без слеша)

Railway интерпретирует `/backend` как абсолютный путь, а `backend` как относительный от корня репозитория.

---

## ✅ После исправления:

Railway будет использовать Railpack, который:
- Автоматически определит Python проект
- Найдет `requirements.txt` в папке `backend`
- Установит зависимости
- Запустит приложение

---

## 🔄 Если нужно вернуть Dockerfile:

Если в будущем захотите использовать Dockerfile:
1. Переименуйте `Dockerfile.backup` обратно в `Dockerfile`
2. Установите Root Directory = `backend`
3. Установите Dockerfile Path = `Dockerfile` (относительный путь)

