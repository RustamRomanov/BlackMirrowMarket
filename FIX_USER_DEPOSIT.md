# Исправление проблемы с депозитом пользователя 8032604270

## Быстрое решение

### Вариант 1: Ручное пополнение через админку (рекомендуется)

1. Откройте админку: `https://ваш-домен.railway.app/admin`
2. Перейдите в раздел **"Балансы"** (User Balance) или используйте прямую ссылку: `/admin/user-balance`
3. Найдите форму **"💰 Пополнить баланс пользователя (ручное)"**
4. Введите:
   - **Telegram ID**: `8032604270`
   - **Сумма (TON)**: сумму, которую пользователь перевел
5. Нажмите **"➕ Пополнить"**

### Вариант 2: Проверка автоматической обработки

1. **Проверьте логи Railway:**
   - Зайдите в Railway → Ваш backend сервис → Logs
   - Найдите сообщения:
     - `✅ Автоматически зачислено` - депозит обработан
     - `⚠️ Ошибка обработки депозита` - была ошибка
     - `TON API error` - проблема с API

2. **Проверьте депозиты в базе данных:**
   - Если у вас есть доступ к PostgreSQL на Railway, выполните:
   ```sql
   -- Найти депозиты пользователя
   SELECT * FROM deposits 
   WHERE telegram_id_from_comment = '8032604270'
      OR user_id = (SELECT id FROM users WHERE telegram_id = 8032604270)
   ORDER BY created_at DESC;
   ```

3. **Если депозит найден, но статус "pending":**
   - Система проверяет депозиты каждую минуту
   - Подождите 1-2 минуты
   - Если не обработалось, используйте Вариант 1 (ручное пополнение)

## Возможные причины проблемы:

1. **Telegram ID не указан в комментарии транзакции**
   - Пользователь должен был указать `8032604270` в комментарии/мемо при переводе
   - Если не указал - депозит не будет автоматически зачислен

2. **Транзакция еще не подтверждена**
   - Нужно подождать подтверждения в блокчейне (обычно 1-2 минуты)

3. **TON API не настроен или недоступен**
   - Проверьте переменные окружения на Railway:
     - `TONAPI_KEY` - должен быть установлен
     - `TON_WALLET_ADDRESS` - адрес сервисного кошелька

4. **Ошибка при обработке**
   - Проверьте логи на ошибки
   - Депозит может быть в статусе "failed"

## Что проверить у пользователя:

1. ✅ Указал ли он Telegram ID (`8032604270`) в комментарии к транзакции?
2. ✅ Правильный ли адрес кошелька использовался? (должен быть из `/api/balance/{telegram_id}/deposit-info`)
3. ✅ Подтверждена ли транзакция в блокчейне? (можно проверить в TON Explorer)
4. ✅ Прошло ли достаточно времени? (1-2 минуты после подтверждения)

## Рекомендация:

**Используйте ручное пополнение через админку** - это самый быстрый способ решить проблему. Автоматическая система работает, но иногда могут быть задержки или ошибки.



