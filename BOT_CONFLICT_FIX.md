# 🔧 Исправление конфликта бота

## Проблема

Ошибка: `Conflict: terminated by other getUpdates request; make sure that only one bot instance is running`

**Причина:** Бот запущен в двух местах одновременно:
- Локально (в терминале)
- На Railway (если вы создали сервис для бота)

Telegram не позволяет запускать несколько экземпляров бота с одним токеном одновременно.

## ✅ Решение

### Вариант 1: Остановите локальный бот (для разработки)

Если вы тестируете локально:
1. Найдите терминал, где запущен `python3 run_bot.py`
2. Нажмите `Ctrl+C` для остановки
3. Теперь бот будет работать только на Railway

### Вариант 2: Остановите бота на Railway (для локальной разработки)

Если хотите работать только локально:
1. Railway → ваш проект → найдите сервис бота
2. Остановите сервис или удалите его
3. Запустите бота локально: `python3 run_bot.py`

### Вариант 3: Используйте разные токены

Если нужны два бота:
- Основной бот: `TELEGRAM_BOT_TOKEN`
- Тестовый бот: `TELEGRAM_ADMIN_BOT_TOKEN` (другой токен)

---

## 🚀 Рекомендация

**Для production:** Запускайте бота только на Railway
**Для разработки:** Запускайте бота только локально

Не запускайте одновременно!



