# 🔧 Ручное переключение Builder в Railway

## Проблема

Railway автоматически определил Dockerfile и не переключается на Railpack автоматически, даже после переименования Dockerfile.

## Решение: Ручное переключение

### Шаг 1: Откройте настройки Build

1. В Railway откройте ваш сервис **BlackMirrowMarket**
2. Перейдите на вкладку **"Settings"**
3. В боковой панели справа нажмите **"Build"**

### Шаг 2: Измените Builder

1. В разделе **"Builder"** вы увидите карточку с **"Dockerfile"** и тегом "Automatically Detected"
2. **Нажмите на эту карточку** - откроется выпадающий список
3. В выпадающем списке выберите **"Railpack"** (с тегом "Default")
4. Или выберите **"Nixpacks"** (если Railpack недоступен)

### Шаг 3: Очистите Dockerfile Path

1. В разделе **"Dockerfile Path"** удалите все содержимое
2. Оставьте поле **ПУСТЫМ**
3. Сохраните (обычно сохраняется автоматически)

### Шаг 4: Проверьте Root Directory

1. Перейдите в раздел **"Source"** (боковая панель справа)
2. Убедитесь, что **Root Directory = `backend`** (без слеша)
3. Если там `/backend` - измените на `backend`

### Шаг 5: Проверьте Start Command

1. Перейдите в раздел **"Deploy"** (боковая панель справа)
2. Убедитесь, что **Start Command:**
   ```
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

### Шаг 6: Сохраните и дождитесь деплоя

1. Все изменения сохраняются автоматически
2. Railway автоматически начнет новый деплой
3. Проверьте вкладку **"Deployments"**
4. Должен пройти успешно!

---

## ⚠️ Если Builder не кликабельный

Если карточка "Dockerfile" не кликается:

1. Попробуйте **обновить страницу** (F5 или Cmd+R)
2. Или **очистите кэш браузера**
3. Или попробуйте в **другом браузере**

---

## 📝 Альтернативный способ

Если Builder все еще не меняется, можно попробовать:

1. **Удалить сервис** и создать заново (но это крайний случай)
2. Или использовать **Railway CLI** для изменения настроек

---

## ✅ После переключения

Railway будет использовать Railpack/Nixpacks, который:
- Автоматически определит Python проект
- Найдет `requirements.txt` в папке `backend`
- Установит зависимости
- Запустит приложение



