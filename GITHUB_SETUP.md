# 📦 Создание репозитория на GitHub

## Шаг 1: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите на "+" в правом верхнем углу → "New repository"
3. Заполните:
   - **Repository name:** `BlackMirrowMarket`
   - **Description:** `Telegram Mini App - Маркетплейс микро-задач с TON блокчейном`
   - **Visibility:** Private (рекомендуется) или Public
   - **НЕ** добавляйте README, .gitignore или лицензию (у нас уже есть)
4. Нажмите "Create repository"

---

## Шаг 2: Подключение локального репозитория

После создания репозитория GitHub покажет инструкции. Выполните в терминале:

```bash
cd /Users/user/BlackMirrowMarket

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/BlackMirrowMarket.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/BlackMirrowMarket.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Запушьте код
git push -u origin main
```

---

## Шаг 3: Проверка

1. Обновите страницу репозитория на GitHub
2. Должны увидеть все файлы проекта
3. Готово! 🎉

---

## 🔐 Если нужна аутентификация

### Вариант 1: Personal Access Token (рекомендуется)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен как пароль

### Вариант 2: SSH ключи

1. Создайте SSH ключ:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. Добавьте публичный ключ в GitHub:
   - Settings → SSH and GPG keys → New SSH key
   - Вставьте содержимое `~/.ssh/id_ed25519.pub`

3. Используйте SSH URL для remote:
```bash
git remote set-url origin git@github.com:YOUR_USERNAME/BlackMirrowMarket.git
```

---

## ✅ После успешного push

Код будет на GitHub и готов для:
- ✅ Деплоя на Railway
- ✅ Автоматических деплоев
- ✅ Совместной работы
- ✅ Резервного копирования

