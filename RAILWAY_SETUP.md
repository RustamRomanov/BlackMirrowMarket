# 🚂 Пошаговая инструкция: Деплой на Railway

## 📋 Что нужно подготовить

1. ✅ GitHub репозиторий с вашим кодом
2. ✅ Аккаунт на Railway (https://railway.app)
3. ✅ Telegram Bot токен (от @BotFather)
4. ✅ TON API ключ и настройки кошелька

---

## 🚀 Шаг 1: Регистрация и подключение GitHub

1. Зайдите на https://railway.app
2. Нажмите "Login" → выберите "Login with GitHub"
3. Разрешите Railway доступ к вашему GitHub аккаунту
4. Railway автоматически подключится к вашим репозиториям

---

## 🗄️ Шаг 2: Создание проекта и базы данных

1. В Railway нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите ваш репозиторий `BlackMirrowMarket`
4. Railway создаст проект и начнет деплой

### Добавление PostgreSQL:

1. В проекте нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически создаст базу данных
4. **Важно:** Запомните или скопируйте:
   - `DATABASE_URL` (будет в переменных окружения)
   - Или отдельные параметры подключения

### Добавление Redis (опционально, но рекомендуется):

1. В проекте нажмите "+ New"
2. Выберите "Database" → "Add Redis"
3. Railway создаст Redis инстанс
4. **Важно:** Запомните `REDIS_URL`

---

## ⚙️ Шаг 3: Настройка Backend

### 3.1 Создание Backend сервиса

1. В проекте нажмите "+ New"
2. Выберите "GitHub Repo"
3. Выберите ваш репозиторий
4. Railway спросит "Configure Service"
5. Укажите:
   - **Root Directory:** `backend`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

### 3.2 Настройка переменных окружения

В настройках Backend сервиса → "Variables" добавьте:

```env
# База данных (Railway автоматически создаст DATABASE_URL, но можно переопределить)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis (если добавили)
REDIS_URL=${{Redis.REDIS_URL}}

# TON настройки
TON_WALLET_ADDRESS=UQCc5ORf-eL7vBXVREuwMNOWq7jOUE--06Jvz95vRQt9dXXF
TON_WALLET_SEED=ваша_seed_фраза_12_слов
TONAPI_KEY=AGBMV6ZNTWRBHIYAAAAIMTWM3FZWBGA2IA775HKX67VIBS5JU6PEDWITLPECOMA2IWXARNQ

# Безопасность
SECRET_KEY=сгенерируйте_случайную_строку_минимум_32_символа
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_надежный_пароль_админа

# Окружение
ENVIRONMENT=production
DEBUG=False

# CORS (важно для Telegram Mini App)
CORS_ORIGINS=https://ваш_домен.railway.app,https://t.me
```

**Как получить SECRET_KEY:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 3.3 Настройка домена

1. В настройках Backend сервиса → "Settings"
2. Найдите "Domains"
3. Нажмите "Generate Domain"
4. Railway создаст домен типа: `ваш-проект-production.up.railway.app`
5. **Запомните этот домен** - он понадобится для Frontend

---

## 🎨 Шаг 4: Настройка Frontend

### 4.1 Создание Frontend сервиса

1. В проекте нажмите "+ New"
2. Выберите "GitHub Repo"
3. Выберите ваш репозиторий
4. Укажите:
   - **Root Directory:** `frontend`
   - **Build Command:** `npm install && npm run build`
   - **Start Command:** `npx serve -s dist -l $PORT`

### 4.2 Настройка переменных окружения

В настройках Frontend сервиса → "Variables" добавьте:

```env
# API URL (замените на домен вашего Backend)
VITE_API_URL=https://ваш-backend-домен.railway.app
```

**Важно:** Используйте домен Backend, который Railway создал на шаге 3.3

### 4.3 Настройка домена Frontend

1. В настройках Frontend сервиса → "Settings"
2. Найдите "Domains"
3. Нажмите "Generate Domain"
4. Railway создаст домен типа: `ваш-проект-frontend.up.railway.app`
5. **Этот домен** будет использоваться для Telegram Mini App

---

## 🗄️ Шаг 5: Инициализация базы данных

### 5.1 Подключение к PostgreSQL

Railway предоставляет несколько способов:

**Вариант A: Через Railway CLI**
```bash
# Установка Railway CLI
npm i -g @railway/cli

# Логин
railway login

# Подключение к проекту
railway link

# Подключение к PostgreSQL
railway connect postgres
```

**Вариант B: Через psql с Railway URL**
```bash
# Получить DATABASE_URL из Railway (Settings → Variables)
# Затем:
psql $DATABASE_URL
```

### 5.2 Создание таблиц

```sql
-- В psql выполните:
\c blackmirrowmarket

-- Создание таблиц (Railway автоматически создаст базу, но нужно создать таблицы)
-- Это можно сделать через Python скрипт или вручную
```

**Или через Python:**
```bash
# В Railway можно использовать "Shell" для выполнения команд
# Или создать отдельный скрипт для инициализации
```

### 5.3 Создание индексов

После создания таблиц выполните:
```bash
# Загрузите database_indexes.sql в Railway
# Или выполните через psql
psql $DATABASE_URL -f database_indexes.sql
```

---

## 🤖 Шаг 6: Настройка Telegram Bot

### 6.1 Создание бота (если еще не создан)

1. Напишите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен бота

### 6.2 Создание Mini App

1. В @BotFather отправьте `/newapp`
2. Выберите вашего бота
3. Заполните:
   - **Title:** BlackMirrowMarket
   - **Short name:** blackmirrowmarket
   - **Description:** Маркетплейс микро-задач
   - **Photo:** загрузите иконку (опционально)
   - **Web App URL:** `https://ваш-frontend-домен.railway.app`

### 6.3 Настройка Webhook (опционально)

Если нужен webhook для бота:
```bash
curl -X POST "https://api.telegram.org/botВАШ_ТОКЕН/setWebhook?url=https://ваш-backend-домен.railway.app/webhook"
```

---

## ✅ Шаг 7: Проверка работы

### 7.1 Проверка Backend

```bash
# Проверка health endpoint
curl https://ваш-backend-домен.railway.app/health

# Должно вернуть: {"status":"healthy"}
```

### 7.2 Проверка Frontend

1. Откройте в браузере: `https://ваш-frontend-домен.railway.app`
2. Должен открыться интерфейс приложения

### 7.3 Проверка в Telegram

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку "Open App" или отправьте `/start`
3. Должно открыться приложение

### 7.4 Проверка админки

1. Откройте: `https://ваш-backend-домен.railway.app/admin`
2. Войдите с логином/паролем из переменных окружения
3. Должна открыться админ-панель

---

## 🔄 Шаг 8: Автоматический деплой

Railway автоматически деплоит при каждом push в GitHub!

**Как это работает:**
1. Вы делаете изменения в коде
2. Коммитите и пушите в GitHub
3. Railway автоматически:
   - Обнаруживает изменения
   - Собирает проект
   - Деплоит новую версию
   - Перезапускает сервисы

**Проверка деплоя:**
- В Railway → "Deployments" видно историю деплоев
- Можно откатить к предыдущей версии (Rollback)

---

## 📊 Мониторинг и логи

### Просмотр логов:

1. В Railway выберите сервис (Backend или Frontend)
2. Вкладка "Deployments" → выберите деплой
3. Вкладка "Logs" - логи в реальном времени

### Метрики:

1. В Railway выберите сервис
2. Вкладка "Metrics" - CPU, RAM, Network

---

## 🔒 Безопасность

### Важные моменты:

1. **Не коммитьте секреты в Git:**
   - Используйте только переменные окружения в Railway
   - Добавьте `.env` в `.gitignore`

2. **Ограничьте доступ:**
   - Используйте сильные пароли
   - Регулярно обновляйте зависимости

3. **Мониторинг:**
   - Следите за логами
   - Настройте алерты (если есть)

---

## 💰 Стоимость

### Бесплатный тариф:
- $5 кредитов в месяц
- Достаточно для тестирования
- Ограниченные ресурсы

### Платные тарифы:
- **Hobby:** $5/месяц + использование
- **Pro:** $20/месяц + использование
- **Для 100k пользователей:** ~$100-300/месяц

**Совет:** Начните с бесплатного, при росте переходите на платный тариф.

---

## 🆘 Решение проблем

### Backend не запускается:

1. Проверьте логи в Railway
2. Проверьте переменные окружения
3. Проверьте, что `DATABASE_URL` правильный
4. Проверьте, что все зависимости установлены

### Frontend не открывается:

1. Проверьте, что `VITE_API_URL` правильный
2. Проверьте логи сборки
3. Проверьте, что `npm run build` успешно выполнился

### База данных не подключается:

1. Проверьте `DATABASE_URL` в переменных окружения
2. Проверьте, что PostgreSQL сервис запущен
3. Проверьте логи PostgreSQL

### CORS ошибки:

1. Убедитесь, что `CORS_ORIGINS` включает домен Frontend
2. Проверьте, что используется HTTPS

---

## 📝 Чеклист

- [ ] Railway аккаунт создан
- [ ] GitHub репозиторий подключен
- [ ] PostgreSQL добавлен
- [ ] Redis добавлен (опционально)
- [ ] Backend сервис создан
- [ ] Frontend сервис создан
- [ ] Переменные окружения настроены
- [ ] Домены созданы
- [ ] База данных инициализирована
- [ ] Индексы созданы
- [ ] Telegram Bot создан
- [ ] Mini App настроен
- [ ] Backend работает (health check)
- [ ] Frontend работает
- [ ] Приложение открывается в Telegram
- [ ] Админка работает

---

## 🎉 Готово!

После выполнения всех шагов ваше приложение будет:
- ✅ Развернуто на Railway
- ✅ Доступно через HTTPS
- ✅ Автоматически деплоится при изменениях
- ✅ Готово к тестированию с реальными пользователями

---

## 📞 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app
- Railway Discord: https://discord.gg/railway

